---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install test dependencies
      ansible.builtin.raw: apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y procps python3
      changed_when: true
